
server:
  port: 0

spring:
  application:
    name: blogservice
  ### ??????????
  profiles:
    active: dev
  ### ??????????
  datasource:
    driver-class-name: org.sqlite.JDBC
    url: jdbc:sqlite:C:\\Users\\Daglemino\\Documents\\db\\blog.db
    username:
    password:
  ### Kafka configurations:
  cloud:
    stream:
      bindings:
        output:
          destination: travelportal.readers
          content-type: application/json
      kafka:
        binder:
          brokers: localhost:9092

  ### sqlite??????????
  jpa:
    database-platform: at.technikumwien.blog.dialect.SQLiteDialect
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka